rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Cho phép đọc/ghi tự do (Dành cho việc Test, sau này làm thật sẽ giới hạn lại)
      allow read, write: if true;
    }
  }
}
